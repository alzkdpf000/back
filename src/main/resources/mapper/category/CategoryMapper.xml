<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.category.CategoryMapper">
    <select id="selectCategoryByPostId">
        select category_name
        from tbl_category join tbl_consultation_post_category
        on tbl_category.id = tbl_consultation_post_category.category_id
        where tbl_consultation_post_category.consultation_post_id = #{consultationPostId}
    </select>
</mapper>